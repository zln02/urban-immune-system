# 이경준 — Backend 담당 온보딩

> 박진영이 Discord 로 이 문서 링크 보내면 그대로 읽고 시작.

## 🎯 당신이 하는 일
**"AI 뇌가 만든 결과를 공무원 화면에 배달하는 창구"** = FastAPI 서버.
이게 없으면 김나영의 예쁜 화면이 빈 페이지로 나옴. 또한 **정부 납품 시 보안 심사 대상** 이라 잘못 만들면 팀 전체가 탈락.

## 🔍 지금 상태
- ✅ `backend/app/main.py` 서버 뜸. `/health` 200 응답 확인됨 (2026-04-21 config 버그 수정 완료)
- 🟡 `api/signals.py`, `alerts.py`, `predictions.py` — 엔드포인트 선언만, 로직 placeholder
- 🔴 JWT 인증 · 감사 로그 · Rate Limiting 없음

## 📋 담당 이슈
👉 **[GitHub Issue #3](https://github.com/zln02/urban-immune-system/issues/3)** — `/api/v1/signals/latest` 실 DB 쿼리 활성화

## ⚡ 오늘 1단계 (복사해서 실행)

```bash
# 1) 서버 접속
ssh wlsdud5035@34.64.124.90
cd ~/urban-immune-system

# 2) 최신 동기화
git fetch origin
git checkout develop
git pull origin develop

# 3) 자기 브랜치
git switch -c feature/ljn-signals-db-query origin/develop

# 4) 백엔드 기동 테스트
source .venv/bin/activate
uvicorn backend.app.main:app --reload --port 8000
# 다른 터미널에서: curl http://localhost:8000/health
# → {"status":"ok","version":"0.2.0"} 뜨면 성공

# 5) Ctrl+C 로 서버 끄고 Claude 시작
cd backend && claude
```

## 🤖 Claude에게 이렇게 말하면 됨

```
이슈 #3 내용대로 backend/app/api/signals.py 의 
/api/v1/signals/latest 엔드포인트를 실 DB 쿼리로 바꿔줘.
조건:
- SQL 은 파라미터 바인딩 (:region 또는 $1 스타일)
- Pydantic 응답 모델 정의 (backend/app/models/signal.py 신규)
- 비동기 SQLAlchemy 세션 사용
- 변경 전에 뭘 바꿀지 먼저 알려줘.
```

## ✅ 다 하면 이렇게 증명

```bash
# 테스트
pytest tests/ -q
ruff check backend/

# 서버 뜨는지
uvicorn backend.app.main:app --port 8000 &
curl "http://localhost:8000/api/v1/signals/latest?region=서울특별시"
# JSON 응답 나오면 OK (DB 비어있으면 빈 배열)

# 커밋 + PR
git add -A
git commit -m "feat(backend): signals/latest 실 DB 쿼리 활성화 (closes #3)"
git push -u origin feature/ljn-signals-db-query
gh pr create --base develop --fill
```

## ⏱ 시간 예상
- 환경 세팅: 30분
- Claude와 엔드포인트 구현: 2-3시간
- 테스트·PR: 30분
**총 3-4시간**. 막히면 30분 이상 끙끙대지 말고 Discord `#uis-help` 에 에러 전문 붙여넣기.

## 🆘 자주 막히는 지점
- **ModuleNotFoundError**: `source .venv/bin/activate` 안 함
- **DB 접속 실패**: `sudo docker compose ps` 로 uis-timescaledb healthy 확인
- **Pydantic 에러**: 이미 해결됨 (2026-04-21 config 버그 수정)
- **gh auth 에러**: `gh auth login --web && gh auth setup-git`

## 📚 참고
- `backend/CLAUDE.md` (역할·핵심 파일 요약)
- `infra/db/init.sql` (DB 스키마)
- `docs/team-explainer.md` (프로젝트 전체 그림)
