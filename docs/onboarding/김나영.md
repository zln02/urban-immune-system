# 김나영 — Frontend 온보딩

## 🎯 당신이 하는 일
**"공무원·심사위원이 가장 먼저 보는 화면"** = Streamlit 대시보드 (Phase 1, 발표 주력) + Next.js (Phase 2, 미래).
아무리 AI 좋아도 **화면 못 쓰면 시스템 쓸모 없음**. 발표 15분 중 3분은 당신 데모.

## 🔍 지금 상태
- ✅ `src/app.py` Streamlit 5탭 뜸 (http://34.64.124.90:8501)
- ✅ validation 탭 · correlation 탭 — JSON 로드 동적 렌더 (P0 완료)
- ✅ report 탭 — 신뢰성 띠 + PDF 다운로드 버튼
- 🟡 지도·리포트 수치 일부 하드코딩
- 🔴 Next.js `frontend/` — 컴포넌트 스텁만, 실 API 미연결

## 📋 담당 이슈
👉 **[GitHub Issue #5](https://github.com/zln02/urban-immune-system/issues/5)** — validation 탭 JSON 연동 검증 + report 탭 PDF 개선

## ⚡ 오늘 1단계

```bash
ssh wlsdud5035@34.64.124.90
cd ~/urban-immune-system
git fetch origin && git checkout develop && git pull
git switch -c feature/nyk-dashboard-demo origin/develop

# Streamlit 기동 (로컬 SSH 터널)
source .venv/bin/activate

# 서버에 SSH 터널로: ssh -L 8501:localhost:8501 wlsdud5035@34.64.124.90
# 이미 서버에서 Streamlit 돌고 있음 (PID 확인):
ps -ef | grep streamlit | grep -v grep

# 안 돌면 직접 기동:
nohup .venv/bin/streamlit run src/app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true > /tmp/streamlit.log 2>&1 &

# 로컬 브라우저: http://localhost:8501 (SSH 터널 후)
# 5탭 전부 클릭해서 렌더 확인
```

## 📸 오늘 1시간 안에 할 일

### 1) 5탭 전부 스크린샷 (데모용)
- `docs/images/screenshots/dashboard_1_riskmap.png`
- `docs/images/screenshots/dashboard_2_timeseries.png`
- `docs/images/screenshots/dashboard_3_correlation.png`
- `docs/images/screenshots/dashboard_4_validation.png`
- `docs/images/screenshots/dashboard_5_report.png`

### 2) 발표 시 보여줄 **Golden Path** 5분 시나리오 암기
1. 지도 탭 → 강남구 클릭 (색맹 안전 팔레트 · 아이콘 3중 코딩 설명)
2. 시계열 → 3-Layer 추이
3. 상관관계 → Granger p-value 실측 강조
4. 검증 → F1=0.643 실측 + 합성 데이터 고지
5. 리포트 → 신뢰성 띠 + **PDF 다운로드 버튼 클릭 시연** (임팩트 최대)

### 3) 깨진 탭 있으면 Claude 에게 수정 요청
```bash
cd src && claude
# "이 탭에서 [에러 메시지] 가 나와. 시뮬 폴백으로라도 렌더되게 고쳐줘."
```

## 🤖 Claude 요청 예시 (이슈 #5)

```
이슈 #5 내용대로:
1. src/tabs/validation.py 의 JSON 로드가 실제 렌더되는지 
   스트림릿 세션에서 테스트
2. src/tabs/report.py 의 PDF 다운로드 실제 동작 검증
3. PDF 에 한국어 폰트 제대로 들어가는지 확인

버그 있으면 고쳐줘. 없으면 "정상 동작 확인" 으로 끝.
```

## ✅ 완성 증명

```bash
# 테스트
pytest tests/ -q
ruff check src/

# 스크린샷 5장 Discord #uis-frontend 에 업로드

# 커밋·PR
git add -A
git commit -m "feat(src): dashboard 5탭 데모 검증 + 스크린샷 (closes #5)"
git push -u origin feature/nyk-dashboard-demo
gh pr create --base develop --fill
```

## ⏱ 시간 예상
- 대시보드 열어보기·스크린샷: 1시간
- 깨진 탭 1개 고치기 (있다면): 1-2시간
- PDF 검증: 30분
**총 2-3시간**. Streamlit 은 쉬운 편이라 막히면 Claude 가 거의 바로 해결.

## 🆘 자주 막히는 지점
- **한국어 폰트 깨짐 (PDF)**: `/usr/share/fonts/truetype/nanum/NanumGothic.ttf` 있어야 함
- **탭 클릭 안 됨**: Streamlit 버전 확인 (`streamlit --version` → 1.56+)
- **JSON 파일 없음**: `python analysis/notebooks/performance_measurement.py` 재실행

## 📅 Phase 2 (Next.js) 는 언제?
**기말발표 이후**. 중간발표까지는 Streamlit 에 집중. Next.js 는 **포기 아닌 보류**로 선언.

## 📚 참고
- `src/CLAUDE.md`
- `src/tabs/` 5개 탭 파일
- `ml/outputs/validation.json` · `correlation.json` (수치 출처)
- `docs/meeting-notes/demo-scripts.md` (3분 스크립트)
