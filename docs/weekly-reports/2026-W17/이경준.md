# 이경준 — 2026-W17 주간 보고서

> 기간: 2026-04-20 ~ 2026-04-26 | 담당: Backend | 모듈: `backend/`

## 1. 이번 주 한 일

- **CORS(다른 도메인에서 우리 서버에 접근할 때의 보안 정책) 설정 개선 작업 시작**: 현재 코드에 `localhost:3000`, `localhost:8501`이 직접 박혀 있어 실제 배포 시 문제 → 환경변수로 이동하는 작업 진행.
- **감사 로그(Audit Log; 누가 언제 어떤 API를 호출했는지 자동 기록하는 보안 기능) 미들웨어 설계 검토**: ISMS-P 인증을 위해 필요. 요청자 IP, 시각, 응답 코드를 DB에 기록하는 구조로 설계.
- **FastAPI(Python으로 만든 웹 API 프레임워크) 라우터별 동작 확인**: `/api/v1/signals/latest`, `/api/v1/signals/timeseries` 두 엔드포인트만 실제 DB 쿼리 구현돼 있고 나머지는 TODO 상태임을 확인.

## 2. 마주친 어려움 · 에러

| # | 증상 | 원인 | 해결 / 현재 상태 |
|---|---|---|---|
| 1 | `backend/app/middleware/` 디렉토리 자체가 없음 | 스캐폴드 미작성 | 새로 만들 예정, 박진영이 구조 가이드 제공 |
| 2 | `/alerts/generate` 엔드포인트가 `TODO: 백그라운드 작업 등록` 주석만 있음 | 실제 로직 미구현 | 다음 주 실제 구현 예정 |
| 3 | JWT(JSON Web Token; 로그인 상태를 암호화해 전달하는 표준) 인증 부재 | 구현 우선순위 밀림 | 스캐폴드부터 시작 |

## 3. 다음 주 계획

- [ ] `backend/app/config.py`에 `ALLOWED_ORIGINS` 환경변수 필드 추가
- [ ] `backend/app/middleware/audit.py` 신설 + `main.py`에 등록
- [ ] PR 최소 2건 생성 (`feature/ljn-cors-env`, `feature/ljn-audit-log`)
- [ ] 박진영과 감사 로그 DB 스키마 확정

## 4. 지표

| 항목 | 값 |
|---|---|
| 커밋 수 | 2 (예상) |
| PR 수 | open 1 / merged 0 |
| 테스트 커버리지 | `backend/` 측정 전 |
| 담당 모듈 변경량 | +50 줄 (예상) |

## 5. 참고 링크

- `docs/weekly-reports/guides/이경준_W17.md` — step-by-step 가이드
- `docs/business/isms-p-checklist.md` — 감사 로그 항목

## 6. KPT

- **Keep**: 이번 주 실제 PR 오픈 의지 — git 커밋 본인 계정으로 직접 진행 예정
- **Problem**: FastAPI 미들웨어 구조 처음 작성 — 학습 곡선 있음
- **Try**: 박진영에게 PR 리뷰 요청 전 로컬에서 `uvicorn` 직접 실행해 동작 확인

## 7. 팀 협업

- 받은 도움: 박진영 step-by-step 가이드, 박정빈 CI 자동 점검
- 준 도움: 백엔드 개발 환경 공유 (다른 팀원이 `/docs` Swagger UI 확인 가능하게)
- 블로커: 없음
