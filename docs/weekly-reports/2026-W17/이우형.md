# 이우형 — 2026-W17 주간 보고서

> 기간: 2026-04-20 ~ 2026-04-26 | 담당: Data Engineer | 모듈: `pipeline/`

## 1. 이번 주 한 일

- **데이터 수집 파이프라인(Kafka: 데이터를 순서대로 안전하게 전달하는 메시지 큐) 구조 점검**: 현재 수집기(Producer)는 작성돼 있지만 받는 쪽(Consumer)이 없어 데이터가 DB에 쌓이지 않음을 확인.
- **네이버 쇼핑인사이트 API 연동 점검**: `pipeline/collectors/otc_collector.py`에서 카테고리 ID `50000008`이 TODO로 표시돼 있어 실제 감기약/해열제 카테고리 ID로 교체 필요함을 확인.
- **KOWAS(환경부 하수감시 시스템) PDF 파싱 로직 점검**: 현재 `pdfplumber` 라이브러리로 파싱 시도하나 실제 PDF의 표 구조에 맞춘 로직이 미완성 상태.

## 2. 마주친 어려움 · 에러

| # | 증상 | 원인 | 해결 / 현재 상태 |
|---|---|---|---|
| 1 | Kafka Consumer 파일 자체가 없음 (`pipeline/consumers/`) | 초기 scaffold 누락 | 박진영이 `timescale_sink.py` 템플릿 제공 → 본인 계정으로 커밋 예정 |
| 2 | 네이버 카테고리 ID 확정 안 됨 | 실제 API 문서 조사 필요 | 다음 주 네이버 개발자센터 카테고리 목록 재확인 |
| 3 | KOWAS 원본 PDF 확보 안 됨 | 환경부 사이트에서 직접 다운로드 필요 | 임시로 2024년 샘플 PDF 1건 확보 후 파싱 PoC |

## 3. 다음 주 계획

- [ ] `pipeline/consumers/timescale_sink.py` 신설 — Kafka → TimescaleDB(시계열 특화 PostgreSQL) 적재
- [ ] `otc_collector.py`의 `50000008` TODO 해결 (네이버 실제 카테고리 ID 확정)
- [ ] 스모크 테스트: 수집기 1회 실행 → Kafka 토픽 확인 → Consumer 통해 DB 적재 확인
- [ ] Prefect(데이터 파이프라인 오케스트레이션 도구) 전환 PoC 검토

## 4. 지표

| 항목 | 값 |
|---|---|
| 커밋 수 | 2 (예상) |
| PR 수 | open 1 / merged 0 |
| 담당 모듈 변경량 | +80 줄 (Consumer 신설) |
| Kafka 토픽 | 4종 정의됨 (`uis.otc`, `uis.search`, `uis.wastewater`, `uis.weather`) |

## 5. 참고 링크

- `docs/weekly-reports/guides/이우형_W17.md` — step-by-step 가이드
- `pipeline/CLAUDE.md` — 모듈 역할 정의
- 네이버 개발자센터: https://developers.naver.com (카테고리 목록 조회)

## 6. KPT

- **Keep**: 기존 수집기 코드를 최대한 재사용, 중복 구현 지양
- **Problem**: 실제 네이버 API 호출 시 429(요청 과다) 응답 대응 로직 미작성
- **Try**: 박진영 제공 Consumer 템플릿을 이해한 뒤 본인 손으로 재작성 (학습 목적)

## 7. 팀 협업

- 받은 도움: 박진영 Consumer 코드 템플릿
- 준 도움: 이경준에게 `/signals` 엔드포인트가 읽는 DB 스키마 설명
- 블로커: 네이버 카테고리 ID 확정 없으면 수집 데이터 의미 없음
